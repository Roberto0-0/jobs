<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Chango&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/@phosphor-icons/web"></script>
  <link rel="stylesheet" href="/css/company/create/style.css">
  <link rel="stylesheet" href="/css/footer/style.css">
  <link rel="stylesheet" href="/css/rootColor/style.css">
  <title>Jobs</title>
</head>
<body>
  <header>
    <div class="container">
      <a href="/" class="logo">Jobs</a>
      <nav class="nav">
        <ul>
          <li>
            <i class="ph-fill ph-key-return"></i>
            <a href="/profile">back</a>
          </li>
        </ul>
      </nav>
      <div class="menu">
        <i class="ph-bold ph-list"></i>
      </div>
    </div>
  </header>
  <main>
    <div class="container">
     <div class="info">
        <h2>Register your <span>company</span> on our website completely <span>free</span> of charge</h2>
     </div>
      <% if(user) { %>
        <form action="/company/registration/<%= user.id %>" method="POST">
          <div class="inputContainer">
            <div class="firstBlock">
              <div class="input">
                <label for="employer">Employer</label>
                <input type="text" name="employer" autocomplete="off" required>
                <% if(error_message.length > 0) { %>
                    <% for(let i=0;i < error_message.length;i++) { %>
                        <% if(error_message[i] == "Employer is required." || error_message[i] == "Employer name is too short.") { %>
                            <div class="error_message">
                                <i class="ph-fill ph-warning"></i>
                                <span class="message"><%= error_message[i] %></span>
                            </div>
                        <% } %>
                    <% } %>
               <% } %>
              </div>
              <div class="input">
                <label for="companyname">Company</label>
                <input type="text" name="companyName" autocomplete="off" required>
                <% if(error_message.length > 0) { %>
                    <% for(let i=0;i < error_message.length;i++) { %>
                        <% if(error_message[i] == "Company name is required." || error_message[i] == "Company name is too short." || error_message[i] == "Company name has already been registered.") { %>
                            <div class="error_message">
                                <i class="ph-fill ph-warning"></i>
                                <span class="message"><%= error_message[i] %></span>
                            </div>
                        <% } %>
                    <% } %>
               <% } %>
              </div>
            </div>
            <div class="secondBlock">
              <div class="input">
                <label for="location">Location</label>
                <input type="text" name="location" autocomplete="off" required>
                <% if(error_message.length > 0) { %>
                    <% for(let i=0;i < error_message.length;i++) { %>
                        <% if(error_message[i] == "location is required." || error_message[i] == "Invalid location.") { %>
                            <div class="error_message">
                                <i class="ph-fill ph-warning"></i>
                                <span class="message"><%= error_message[i] %></span>
                            </div>
                        <% } %>
                    <% } %>
               <% } %>
              </div>
              <div class="input">
                <label for="email">Email</label>
                <input type="email" name="email" class="email-input" autocomplete="off" required>
                <% if(error_message.length > 0) { %>
                    <% for(let i=0;i < error_message.length;i++) { %>
                        <% if(error_message[i] == "Email is required." || error_message[i] == "Invalid email.") { %>
                            <div class="error_message">
                                <i class="ph-fill ph-warning"></i>
                                <span class="message"><%= error_message[i] %></span>
                            </div>
                        <% } %>
                    <% } %>
               <% } %>
              </div>
            </div>
          </div> 
          <div class="input">
            <label for="aboutCompany">About your company</label>
            <textarea name="aboutCompany" id="aboutCompany" cols="30" rows="10" required></textarea>
            <% if(error_message.length > 0) { %>
                    <% for(let i=0;i < error_message.length;i++) { %>
                        <% if(error_message[i] == "This field is required.") { %>
                            <div class="error_message">
                                <i class="ph-fill ph-warning"></i>
                                <span class="message"><%= error_message[i] %></span>
                            </div>
                        <% } %>
                    <% } %>
               <% } %>
          </div>
          <button>create company</button>
        </form>
      <% } %> 
    </div>
  </main>
   <footer>
    <div class="container">
      <p>
        &copy; 2022 <a href="https://pt.wikipedia.org/wiki/Direito_autoral">Copyright</a>™ All rights reserved.
      </p>
    </div>
  </footer>
    <script src="/js/company/main.js"></script>
    <script src="/js/menu/main.js"></script>
  </body>
</html>